# 仓库使用规范

## 1. 仓库边界

本仓库是文档和实验记录仓库，不是 BSP 源码镜像。

允许保存：

- Markdown 技术文档；
- 小型配置片段和补丁；
- 串口、dmesg、perf、ftrace、media-ctl 等实验日志；
- CSV、SVG、PNG 等测试结果；
- 为解释问题而提取的小型代码片段。

不应保存：

- 完整 Linux SDK、rootfs、固件镜像；
- 大型编译产物；
- 第三方课程和书籍副本；
- 私钥、Token、密码和公司机密。

## 2. 目录归档规则

### `docs/`

保存经过整理、能够复用的知识和结论。一个问题只有形成了完整证据链后，才从日记提升为正式文档。

### `journal/`

每天一个文件，命名为 `YYYY-MM-DD.md`。日记允许保留失败尝试，但必须区分：

- 已观察到的事实；
- 当前假设；
- 尚未验证的推断；
- 最终结论。

### `artifacts/`

保存不应被人工改写的原始证据。文件名必须包含日期或实验 ID，例如：

```text
artifacts/boot-logs/2026-07-30-kernel-baseline.log
artifacts/media/2026-08-02-media0-topology.txt
```

若文件大于 10 MiB，优先压缩、裁剪或改用 Git LFS；不得直接提交大型固件。

### `results/`

- `raw/`：原始输出，不手工修改。
- `summary/`：由原始数据计算出的 CSV、表格、图和结论。

性能结论必须能追溯到 raw data、测试环境和源码 commit。

## 3. 实验编号

建议使用：

```text
EXP-YYYYMMDD-NN
```

例如：

```text
EXP-20260805-01：OV5695 1080P NV12 四缓冲采集
```

同一编号应出现在：

- 当日日记；
- 实验记录；
- 原始结果目录；
- 汇总表；
- 相关 Git commit。

## 4. 提交流程

```bash
git status --short
git diff
git diff --check
git add <明确文件>
git diff --cached
git commit -m "<type>(<scope>): <summary>"
git push
```

避免无差别使用 `git add .`。原始日志尤其要先检查敏感信息和文件大小。

## 5. 结论等级

| 等级 | 含义 |
|---|---|
| 推测 | 尚未通过实验验证 |
| 初步验证 | 单次或少量样本符合预期 |
| 已验证 | 有可复现步骤、对照和重复实验 |
| 长稳验证 | 通过规定时长/次数的稳定性测试 |
| 已归档 | 当前阶段不再修改，保留历史 |

简历只引用“已验证”或“长稳验证”的结论和数字。

## 6. 文档链接

- 仓库内部使用相对链接。
- 指向 BSP 源码时同时记录源码仓库和 commit，不依赖某台电脑的绝对路径。
- 文档中引用原始日志时，链接到 `artifacts/` 或 `results/raw/`。

## 7. AI 辅助内容

AI 可以帮助组织计划、解释源码和生成模板，但：

- AI 生成的命令必须经过人工检查；
- AI 推断不能替代板端日志；
- 未执行的操作不得写成已完成；
- 未测量的数据不得写进简历；
- 最终文档要能由本人解释。

